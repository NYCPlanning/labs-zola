import trackEvent from 'labs-zola/utils/track-event';
import { module, test } from 'qunit';

module('Unit | Utility | track event');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trackEvent();
  assert.ok(result);
});
