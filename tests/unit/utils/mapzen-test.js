import mapzen from 'labs-zola/utils/mapzen';
import { module, test } from 'qunit';

module('Unit | Utility | mapzen');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mapzen();
  assert.ok(result);
});
