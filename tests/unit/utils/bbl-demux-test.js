import bblDemux from 'labs-zola/utils/bbl-demux';
import { module, test } from 'qunit';

module('Unit | Utility | bbl demux');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bblDemux();
  assert.ok(result);
});
