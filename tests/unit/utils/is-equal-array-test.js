import isEqualArray from 'labs-zola/utils/is-equal-array';
import { module, test } from 'qunit';

module('Unit | Utility | is-equal-array', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = isEqualArray();
    assert.ok(result);
  });
});
