{{#if (or loadStateTask.isRunning (not map))}}
  <FaIcon @icon='spinner' class='fa-spin fa-3x map-loading-spinner' />
{{/if}}

{{#if this.map}}
  {{maplibre-gl-on "render" (action "handleMapLoading") eventSource=map}}
  {{maplibre-gl-on "data" (action "handleMapLoading") eventSource=map}}
  {{maplibre-gl-on "sourcedata" (action "handleMapLoading") eventSource=map}}
{{/if}}
