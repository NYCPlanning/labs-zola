{{#labs-map
  mapLoaded=(action this.handleMapLoaded)
  id=this.name
  layerGroups=this.layerGroups
  initOptions=this.initOptions as |map|
}}
  {{yield (hash
    instance=this.mapInstance
    hoveredFeature=this.hoveredFeature
  )}}
{{/labs-map}}
