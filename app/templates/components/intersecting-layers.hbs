{{!-- Yield out if the task is done running and if it's not node-side fastboot --}}
{{#if this.intersectingLayers.isRunning}}
<FaIcon @icon='spinner' class='fa-spin fa-3x loading-spinner' />
{{else}}
  {{yield this.intersectingLayers.value this.numberIntersecting}}
{{/if}}
