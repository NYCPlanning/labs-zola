{{!-- Yield out if the task is done running and if it's not node-side fastboot --}}
{{#if (not intersectingLayers.isRunning)}}
  {{yield intersectingLayers.value numberIntersecting}}
{{else}}
  <ContentPlaceholders as |placeholder|>
    <placeholder.text @lines={{3}} />
  </ContentPlaceholders>
{{/if}}
