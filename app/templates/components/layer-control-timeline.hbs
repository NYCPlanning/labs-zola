{{range-slider start=start
               min=min
               max=max
               connect=true
               step=1
               behaviour="drag"
               on-change="sliderChanged"
}}
{{yield}}
