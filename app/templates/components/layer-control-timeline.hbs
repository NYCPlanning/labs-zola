{{range-slider start=start
               min=min
               max=max
               tooltips=(array format format)
               connect=true
               step=1
               behaviour="drag"
               on-change="sliderChanged"
}}
{{yield}}
