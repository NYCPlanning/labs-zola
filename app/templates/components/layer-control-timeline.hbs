<RangeSlider
  @start={{this.start}}
  @min={{this.min}}
  @max={{this.max}}
  @tooltips={{array this.format this.format}}
  @connect={{true}}
  @step={{1}}
  @behaviour="drag"
  @on-change={{action this.sliderChanged}}
/>
{{yield}}