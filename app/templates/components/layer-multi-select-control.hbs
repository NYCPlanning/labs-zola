{{yield (hash
  checkbox = (component
    'layer-checkbox'
    selectionChanged = (action 'selectionChanged')
    parentComponent = this
  )
  selectionChanged = (action 'selectionChanged')
  allChecked = allChecked
  )
}}
