<button class="save-button bookmark-save-button {{if saved 'saved'}}" onclick={{action 'toggleSaved'}}>
  {{fa-icon "bookmark"}}
  <span class="state">{{#if saved}}SAVED{{/if}}</span>
</button>
{{yield}}
