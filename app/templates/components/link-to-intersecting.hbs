{{#if hasBlock}}
  {{#if (await resultingFeature) as |id|}} 
    {{yield (hash resultingFeature=id)}}
  {{/if}}
{{else}}
  {{#if (await resultingFeature) as |id|}} 
    {{#if id}}    
      {{link-to 'Special District' route id classNames=classNames}}
    {{/if}}
  {{/if}}
{{/if}}
