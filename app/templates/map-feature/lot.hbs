<CartoDataProvider
  @modelId={{model.id}}
  @modelName="lot" as |lotModel|
>
  <Mapbox::MapFeatureRenderer
    @model={{lotModel}} />
  <BookmarkButton
    @bookmarkableModel={{lotModel}} />
  <LayerRecordViews::TaxLot
    @model={{lotModel.properties}} />
</CartoDataProvider>
